import React from "react";
import styles from "./featured.module.css";

const Featured = (): React.ReactNode => {
  return <div className={styles.container}>Featured</div>;
};

export default Featured;
